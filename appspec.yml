# codeDeploy 버전
version: 0.0
os: linux
files:
  - source: /  # codeDeploy에서 전달해준 파일중 destination으로 이동시킬 대상을 지정, 루트경로를 지정하면 전체파일을 말함
    destination: /home/ec2-user/app/step2/zip/  # source에서 지정된 파일을 받을 위치
    overwrite: yes  # 기존의 파일을 덮어쓸지에 대한 여부